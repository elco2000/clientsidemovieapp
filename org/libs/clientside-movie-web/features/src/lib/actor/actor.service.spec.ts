import { ActorService } from "./actor.service";
import { TestBed } from "@angular/core/testing";

const testActor = {
    _id: 1,
    name: 'Elco Mussert',
    birthdate: new Date(),
    nationality: 'Dutch',
    photo: 'test.url'
}

describe('ActorService', () => {
    
})